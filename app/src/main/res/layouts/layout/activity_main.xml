<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
        xmlns:android="http://schemas.android.com/apk/res/android"
        xmlns:tools="http://schemas.android.com/tools"
        xmlns:app="http://schemas.android.com/apk/res-auto"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        tools:context=".MainActivity">
    <LinearLayout
            android:orientation="vertical"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintTop_toTopOf="parent"
    >
        <ImageView
                android:layout_width="match_parent"
                android:layout_height="wrap_content" tools:srcCompat="@drawable/loginlogo1"
                android:id="@+id/loginlogo" android:adjustViewBounds="false"
                android:cropToPadding="false"
                android:layout_above="@+id/login_pagebtn"
                android:layout_alignParentLeft="false"
                android:layout_alignParentTop="false"
                android:src="@drawable/loginlogo1"
                android:layout_marginTop="30dp"/>
        <LinearLayout
                android:orientation="vertical"
                android:layout_width="match_parent"
                android:layout_height="match_parent" android:layout_marginTop="40dp">

            <com.google.android.material.button.MaterialButton
                    android:text="Log In"
                    tools:textAllCaps="false"
                    android:layout_width="match_parent"
                    android:layout_height="60dp" android:id="@+id/login_pagebtn" android:layout_weight="1"
                    android:layout_marginStart="30dp" android:layout_marginEnd="30dp"
                    android:layout_marginBottom="0dp" android:textAllCaps="false"
                    android:backgroundTint="@color/maincolor"
                    app:cornerRadius="25dp"
                    style="@style/Widget.MaterialComponents.Button.Icon"
                    app:icon="@drawable/login_white_24dp"
                    app:iconTint="@color/white"
                    android:textColor="@color/white"
                    tools:textSize="20sp"/>

            <com.google.android.material.button.MaterialButton
                    android:text="Sign Up"
                    tools:textAllCaps="false"
                    android:layout_width="match_parent"
                    android:layout_height="60dp" android:id="@+id/signup_pagebtn" android:layout_weight="1"
                    android:layout_marginStart="30dp" android:layout_marginEnd="30dp" android:layout_marginTop="20dp"
                    android:layout_marginBottom="0dp" android:textAllCaps="false"
                    android:backgroundTint="@color/maincolor"
                    app:cornerRadius="25dp"
                    style="@style/Widget.MaterialComponents.Button.Icon"
                    app:icon="@drawable/app_registration_white_24dp"
                    app:iconTint="@color/white"
                    android:textColor="@color/white"
                    tools:textSize="20sp"/>


        </LinearLayout>

        <ImageView
                android:layout_width="match_parent"
                android:layout_height="250dp"
                android:src="@drawable/layer_1_2"
                android:layout_marginTop="25dp"/>

    </LinearLayout>
</androidx.constraintlayout.widget.ConstraintLayout>